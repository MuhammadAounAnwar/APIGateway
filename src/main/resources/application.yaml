server:
  port: 8762

spring:
  application:
    name: api-gateway

  config:
    import: optional:file:.env  # loads environment variables from .env

security:
  jwt:
    public-key: ${AUTH_SERVICE_PUBLIC_KEY:}

  cloud:
    gateway:
      default-filters:
        # Adds correlation id if missing
        - AddRequestHeader=X-Gateway, API-GATEWAY
        - AddResponseHeader=X-Gateway-Version, v1
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
        - RemoveResponseHeader=Server

      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"

      routes:

        # ------------------------------------------------------------------
        # Route 1: Authentication Service (PUBLIC)
        # auth-client controller is at /api/auth/** — no path rewrite needed
        # ------------------------------------------------------------------
        - id: auth_service
          uri: lb://auth-client
          predicates:
            - Path=/api/auth/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${AUTH_RATE_LIMIT:10}
                redis-rate-limiter.burstCapacity: ${AUTH_BURST_CAPACITY:20}
            - name: Retry
              args:
                name: default
            - name: CircuitBreaker
              args:
                name: default
                fallbackUri: forward:/fallback/auth-service

        # ------------------------------------------------------------------
        # Route 2: User Service (SECURED)
        # ------------------------------------------------------------------
        - id: user_service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
          filters:
            - RewritePath=/api/users/(?<segment>.*), /${segment}
            - name: AuthPreFilter
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${USER_RATE_LIMIT:20}
                redis-rate-limiter.burstCapacity: ${USER_BURST_CAPACITY:40}
            - name: Retry
              args:
                name: default
            - name: TimeLimiter
              args:
                name: default
            - name: Bulkhead
              args:
                name: default
            - name: CircuitBreaker
              args:
                name: default
                fallbackUri: forward:/fallback/user-service

        # ------------------------------------------------------------------
        # Route 3: Restaurant Service (SECURED)
        # ------------------------------------------------------------------
        - id: restaurant_service
          uri: lb://RESTAURANT-SERVICE
          predicates:
            - Path=/api/restaurants/**
          filters:
            - RewritePath=/api/restaurants/(?<segment>.*), /${segment}
            - name: AuthPreFilter
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${RESTAURANT_RATE_LIMIT:30}
                redis-rate-limiter.burstCapacity: ${RESTAURANT_BURST_CAPACITY:60}
            - name: Retry
              args:
                name: default
            - name: TimeLimiter
              args:
                name: default
            - name: Bulkhead
              args:
                name: default
            - name: CircuitBreaker
              args:
                name: default
                fallbackUri: forward:/fallback/restaurant-service

        # ------------------------------------------------------------------
        # Route 4: Order Service (SECURED)
        # ------------------------------------------------------------------
        - id: order_service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/api/orders/**
          filters:
            - RewritePath=/api/orders/(?<segment>.*), /${segment}
            - name: AuthPreFilter
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${ORDER_RATE_LIMIT:15}
                redis-rate-limiter.burstCapacity: ${ORDER_BURST_CAPACITY:30}
            - name: Retry
              args:
                name: orderStrict
            - name: TimeLimiter
              args:
                name: orderStrict
            - name: Bulkhead
              args:
                name: orderStrict
            - name: CircuitBreaker
              args:
                name: orderStrict
                fallbackUri: forward:/fallback/order-service

        # ------------------------------------------------------------------
        # Route 5: Notification Service (SECURED)
        # notificationservice controller is at /api/notifications — no rewrite
        # ------------------------------------------------------------------
        - id: notification_service
          uri: lb://notificationservice
          predicates:
            - Path=/api/notifications/**
          filters:
            - name: AuthPreFilter
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${NOTIFICATION_RATE_LIMIT:25}
                redis-rate-limiter.burstCapacity: ${NOTIFICATION_BURST_CAPACITY:50}
            - name: Retry
              args:
                name: default
            - name: TimeLimiter
              args:
                name: default
            - name: Bulkhead
              args:
                name: default
            - name: CircuitBreaker
              args:
                name: default
                fallbackUri: forward:/fallback/notification-service

        # ------------------------------------------------------------------
        # Route 6: Email Service (INTERNAL / SECURED)
        # emailservice controller is at /api/mails — no rewrite
        # ------------------------------------------------------------------
        - id: email_service
          uri: lb://emailservice
          predicates:
            - Path=/api/mails/**
          filters:
            - name: AuthPreFilter
            - name: Retry
              args:
                name: default
            - name: TimeLimiter
              args:
                name: emailLenient
            - name: Bulkhead
              args:
                name: emailLenient
            - name: CircuitBreaker
              args:
                name: emailLenient
                fallbackUri: forward:/fallback/email-service

        # ------------------------------------------------------------------
        # Route 7: Chat Service REST (SECURED)
        # chatservice controller is at /api/v1/chat — no rewrite
        # ------------------------------------------------------------------
        - id: chat_service
          uri: lb://CHAT-SERVICE
          predicates:
            - Path=/api/v1/chat/**
          filters:
            - name: AuthPreFilter
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: ${CHAT_RATE_LIMIT:50}
                redis-rate-limiter.burstCapacity: ${CHAT_BURST_CAPACITY:100}
            - name: Retry
              args:
                name: default
            - name: TimeLimiter
              args:
                name: default
            - name: Bulkhead
              args:
                name: default
            - name: CircuitBreaker
              args:
                name: default
                fallbackUri: forward:/fallback/chat-service

        # ------------------------------------------------------------------
        # Route 8: Chat Service WebSocket (PUBLIC — JWT validated in handler)
        # ------------------------------------------------------------------
        - id: chat_service_ws
          uri: lb://CHAT-SERVICE
          predicates:
            - Path=/ws/chat/**

logging:
  level:
    org.springframework.cloud.gateway: INFO
    reactor.netty.http.client: INFO
    io.github.resilience4j: INFO
    io.opentelemetry: DEBUG
  pattern:
    level: "%5p [${spring.application.name:},traceId=%X{traceId:-},spanId=%X{spanId:-}]"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator

  observations:
    key-values:
      service.name: api-gateway
      service.version: 1.0.0
      deployment.environment: ${spring.profiles.active}

  metrics:
    tags:
      application: api-gateway
    enable:
      reactor.netty.http.client: true
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.9, 0.95, 0.99

  endpoint:
    health:
      show-details: always

  tracing:
    sampling:
      probability: 1.0
    propagation:
      type: W3C
    enabled: true

  otlp:
    tracing:
      endpoint: http://localhost:4317

ono:
  observability:
    logging:
      enabled: true
    exceptions:
      enabled: false
    performance:
      enabled: true
